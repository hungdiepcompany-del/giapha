# Execution plans

`active/` contains exactly one current, real task-specific execution contract. `completed/` contains closed historical contracts. A task may not begin if active-contract count is not exactly one.
